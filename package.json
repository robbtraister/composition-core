{
  "name": "@composition/core",
  "version": "0.0.1-beta.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "prettier-standard {bin,env,src}/**/*.js",
    "test": "npm run lint",
    "preversion": "npm run test",
    "postversion": "git push && git push --tags && (if [ -z \"$(node -p 'require(`./package.json`).version' | grep '-')\" ]; then npm publish; else npm publish --tag \"$(node -p 'require(`./package.json`).version' | sed -e 's/.*-//' -e 's/\\..*//')\")"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bin": {
    "composition": "./bin/composition.js"
  },
  "dependencies": {
    "debug": "^4.1.1",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "lodash.merge": "^4.6.2"
  },
  "devDependencies": {
    "prettier-standard": "^9.1.1"
  }
}
